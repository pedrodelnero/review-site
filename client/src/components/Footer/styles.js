import { makeStyles } from '@material-ui/core/styles';


export default makeStyles((theme) => ({
  root: {
    position: 'fixed',
    left: 0,
    bottom: 0,
    width: '100%',
    height: '100px',
    backgroundColor: 'black',
    color: 'white',
    textAlign: 'center',
  },

}));